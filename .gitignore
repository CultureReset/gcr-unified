# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel

# Vercel CLI writes a short-lived OIDC token here on every `vercel` run.
# It expires in hours, but it is a live credential while it lasts and this
# repo is public — never let one land in a commit.
.env.vercel
.env*.local
